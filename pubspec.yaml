name: just_one_ten_mins
description: 10분 요약 오디오북 앱

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # 기본 위젯
  cupertino_icons: ^1.0.2
  
  # 상태 관리
  flutter_riverpod: ^2.4.9
  
  # 라우팅
  auto_route: ^7.7.1
  
  # 다국어 지원
  intl: ^0.19.0
  
  # win32 버전 문제 해결
  win32: 5.0.7
  collection: ^1.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  
  # 코드 생성기
  build_runner: ^2.4.6
  auto_route_generator: ^7.2.0

flutter:
  uses-material-design: true
  generate: true